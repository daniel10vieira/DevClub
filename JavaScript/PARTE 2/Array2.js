/*  Array - splice, slice, pop e shift */

// splice ( unir / ligar)
// slice (fatiar)
// pop
// shift

const students = ['João', 'Maria', 'Pedro', 'Carla', 'Bia', 'Mauricio']

// students.splice(1, 2)// - Retira o item que deseja na posição que definir

// students.splice(1, 0, 'Guilherme')// - Adiciona o item que quiser na posição que deseja

// const newStudents = students.slice(1,5)// - Cria um novo array com os item que declarar para retirar, por isso da variável

// students.pop()// - Tira o último item do Array

// students.shift()// - Tira o primeiro item do array

console.log(students)